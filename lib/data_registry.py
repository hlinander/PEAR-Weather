"""
Minimal data registry for weather datasets.
Weather datasets register themselves via register_dataset().
"""


def register_datasets():
    """Return empty dict - datasets register themselves via data_factory."""
    return dict()
